# Website Coördinator AMMA — Stephanus Harsono

Teksten staan los van de layout:
- `src/content/site.js` — overzichtspagina (ook `siteUrl`, `cvBeschikbaar`, `cvBestand`)
- `src/content/brief.js` — volledige motivatiebrief (v36)

Lokaal: `npm install && npm run dev` (http://127.0.0.1:4321). Build: `npm run build`. Nog niet gedeployed.
